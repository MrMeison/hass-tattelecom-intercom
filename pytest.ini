[pytest]
asyncio_mode = auto
# Hard per-test deadline: a spinning loop has to fail the test instead of
# hanging the whole run (it used to starve the event loop and the machine).
# signal method interrupts the test and lets the run finish with a summary.
timeout = 60
timeout_method = signal
